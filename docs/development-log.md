# Development log

This is the chronological record of material build decisions. Focused ADRs contain deeper trade-off analysis; this log explains how each step contributes to an autonomously assessable game.

## 2026-08-23

1. Initialized an empty workspace as a `main`-branch Git repository. This makes each coherent decision reviewable and publishable instead of leaving one opaque final dump.
2. Selected Vite, strict TypeScript, Canvas, Vitest, and Playwright. Vite keeps the browser host small; strict types stabilize JSON contracts; Canvas exposes exact render control; Vitest runs the simulation without a browser; Playwright checks the real input and pixels.
3. Pinned dependency versions and generated a lockfile. Reproducible tools and Chromium are necessary for meaningful screenshot baselines.
4. Generated original Cinderwake three-hero key art with the built-in image generator and stored it locally under `public/assets`. It gives character selection a coherent identity without introducing a network dependency or copying an existing franchise.
5. Chose code-rendered runtime actors for the first slice. Explicit shapes make every frame, pivot, hitbox, bound, and foot anchor inspectable; generated raster animation can replace them later under the same manifest contract.
6. Delegated independent game-design, engine-architecture, test-architecture, documentation/CI, browser-rendering, and test-harness passes. Bounded ownership parallelizes work without allowing agents to overwrite the same files.
7. Fixed simulation at 60 Hz and 1,024 integer units per tile. Exact tick advancement removes wall-clock flakiness; integer positions prevent long-run drift.
8. Split random state by domain and made loot entity-keyed. Cosmetic changes cannot perturb gameplay, and killing enemies in a different order cannot silently change a particular enemy's drop.
9. Implemented complete ScenarioV1 construction instead of live-state patching. Arbitrary setups remain possible while reset semantics stay reliable.
10. Added a connected seeded room-and-corridor generator with a digest. Generated-world behavior can be fuzzed across seeds and reproduced from one short value.
11. Added three player archetypes and three enemies with melee, ranged, area, projectile, health, armor, death, loot, pickup, exit, win, and loss state. This is the smallest system mix that exercises both behavior and temporal rendering.
12. Defined two observable contracts: canonical state and semantic render manifest. Pixels show appearance; manifests explain geometry and timing when appearance changes.
13. Defined exact-tick PNG sequence capture and a self-contained report. An agent or human can review motion as a strip while automated checks measure anchors, bounds, speed, frame order, and camera movement.
14. Added a browser Test Lab for scenario loading, manual stepping, JSON export, and sequence preview. The same observability used in CI is available during ordinary development.
15. Added Prettier, ESLint, strict TypeScript, and a combined source-quality command. Authored code remains readable, and the production build now disables minification and emits source maps so the public artifact is inspectable rather than only the repository source.
16. Isolated browser tests on a dedicated strict port with server reuse disabled. An earlier run had silently attached to an unrelated process on Vite's default preview port; identity and isolation now form part of the harness contract.
17. Added real keyboard and pointer adapter coverage alongside semantic bridge tests. This proves that deterministic input injection and the controls a player actually uses reach the same simulation boundary.
18. Added reviewed screenshot baselines for selection, walking, combat impact, loot, and mid-action injection. The first visual review found an overlapping selection title and stale run-log data; both were corrected before preserving the baseline.
19. Added close-up sequence reports with a visible foot-anchor crosshair and machine-readable measurements. They retain exact full frames, state history, manifest history, metadata, and a reproduction command instead of reducing temporal evidence to one screenshot.
20. Visual review found a one-frame seam where the attack clip could wrap to its opening pose before returning to idle. One-shot frames now clamp at the terminal frame, and weapon recovery converges to the idle pose; a test fixes the terminal attack frame and subsequent idle frame in the contract.
21. Added public GitHub Pages assembly. CI rebuilds the readable game, executes source/unit/browser/visual gates, captures walking and attack reports, and publishes the game, browser report, and temporal evidence from the same commit.

Future entries should record any baseline update, tolerance change, schema migration, or intentional animation change. Those decisions directly alter what the quality system accepts and therefore must never be silent.

## 2026-08-23 (continued)

22. Added exact full-`GameState` loading and reset documentation alongside ScenarioV1. Persisting a post-setup world plus its semantic tape eliminates the temptation to reproduce an intermittent defect by manually replaying setup; reset reconstructs the retained source instead of leaking live input or timers.
23. Defined presentation interpolation and camera modes as observable render data. The live display can be smooth without weakening exact-tick captures because manifests state simulation tick, presentation tick, alpha, target, and camera mode.
24. Promoted isolated transparent entity masks from a diagnostic implementation detail to quality evidence. Alpha-pixel bounds, centroid, bottom offset, and hash provide a real raster witness for pivot, silhouette, proportion, and clipping claims.
25. Documented capture profiles, reproducibility bundles, environment metadata, mobile controls/layout, and the public capture matrix. An autonomous report is useful only when a reviewer can rerun its exact state/control/frame sequence and understand what coverage it represents.
26. Ran the first 20-sequence temporal matrix. It exposed an 86-degree enemy death rotation with roughly 23 px of ground drift and a 1.3 px Hexer recovery pop. The death pose now collapses onto the foot plane, and the Hexer attack float curve returns exactly to idle; both defects became permanent profile checks.
27. Distinguished north/south hero raster silhouettes with front/back detail while keeping all four directional masks unique and grounded. This makes control direction visually legible and gives the mask-hash test a concrete contract. Reviewed and updated only the two pixel baselines affected by this intentional directional detail.
28. Reworked the portrait stage to fill the viewport above the touch controls by center-cropping the fixed logical canvas. The same canvas bounding box still maps touch aim into logical coordinates, while an automated layout assertion prevents the prior large blank gap from returning.
29. Adopted a fully sprite-driven presentation contract with title-only text, asset provenance, deterministic atlas manifests/loading, and sprite-specific temporal QA. This converts visual quality from an opaque canvas result into inspectable source frames, anchors, alpha masks, and reproduction bundles.
30. Replaced two hand-picked public captures with a cataloged 20-report CI matrix. Each entry publishes its verdict, category, profile, source scenario, report, mask/state/tape bundle, and exact reproduction command; terminal profiles show their composed win/loss page directly in the contact sheet.
31. Required an independent visual verdict after the first matrix passed 20/20. The reviewer still rejected the loss-modal timing, low-readability Ashfang/Stonekin attacks, and missing projectile-impact coverage. Automated success therefore remains necessary but cannot approve gestalt by itself.
32. Delayed the loss modal until the exact player death lock ends, preserving every death frame on the real composed page. Added deterministic target impact bursts and hurt tint, a readable Ashfang wind-up/lunge, and a Stonekin raise/slam/recoil. The one-shot profile now requires at least five distinct real raster masks, turning the readability finding into a regression gate without loosening continuity limits. Visual review accepted the new motion, after which only the combat-impact, injected-mid-action, and portrait view baselines affected by those intentional pixels were updated.
33. Added a projectile-impact scenario that retains approach through state tick 18, proves exactly one hit/effect, and requires contiguous absence from tick 19. The public matrix now has 21 entries because continuous travel and collision/despawn answer different questions and should not be conflated.
34. The first run of the new five-pose one-shot gate rejected Ranger and Arcanist primaries at 19/21 overall: both advanced through six semantic frames but produced only three distinct raster masks. Ranger now exposes a drawn-bow release and diminishing recoil, while Arcanist visibly charges, releases, and dissipates an asymmetric staff ring. Their terminal masks still equal idle exactly; the gate was kept unchanged.
35. Replaced character-specific cropping assumptions with `ActorAtlasV2`: four fixed 4 × 4 source families, shared normalization, stable bottom-center anchors, explicit facing banks, two reserved atlas rows, and build hashes. A new actor now enters the same pipeline by ID and source sheets instead of acquiring custom renderer logic.
36. Rejected cross-dissolved locomotion and action intermediates after mask strips exposed duplicate bodies, detached Ranger arrows, and recovery ghosts. Authored direction, action-detail, and reaction banks now provide discrete poses; exact primary-idle terminal frames make recovery seams testable rather than subjective.
37. Replaced repeated full-opacity floor tiles with a continuous authored ground plane and combined scenery/actor bottom-depth sorting. This keeps the scene sprite-only while removing the checkerboard floor, allowing actors to pass logically in front of or behind buildings and props.
38. Rebuilt the responsive interface from sprite panels, portraits, controls, icons, and glyphs, retaining DOM text only for titles and accessibility mirrors. Word-grouped glyph layout and portrait/landscape assertions prevent clipped labels, off-stage cards, and touch controls covering play.
39. Kept ordinary pose dimension limits and added a narrow attached-effect rule: a one-shot flash may use a larger silhouette envelope only while centroid step and acceleration stay substantially tighter. Projectile spawn/expiry metadata, camera-normalized masks, quarter-tick mobile samples, and melee subject spacing were corrected at the same evidence boundary; targeted projectile, camera, desktop action, and mobile action captures all pass.
40. A post-matrix browser run exposed that Playwright cleans its `test-results/` root and could erase finished temporal evidence. Moved sequence bundles to ignored `quality-results/sequences/`, updated reproduction paths and CI publication, and added a contract assertion that sprite-rendered HUD values expose both accessibility labels and real glyph images.
41. Baseline review found the selection portraits still cropped ActorAtlasV2 as an 8 × 8 sheet, leaking a second animation row below every hero. Corrected the CSS crop to the real 8 × 12 atlas geometry before accepting any new screenshot.
42. An independent contract audit found ordinary DOM text still visible in labels, buttons, counters, loading, outcomes, and Test Lab despite the title-only rule. Converted every non-title surface to the committed glyph atlas, synchronized an invisible accessible seed input with visible sprite glyphs, replaced the native scenario selector with a sprite chooser, and added a browser text-node audit. The selection Test Lab control now participates in mobile flow instead of covering the Arcanist card.
43. Independent sequence review reproduced a 90-degree pose snap: north/south locomotion was authored, but every attack, ability, hurt, and death clip silently fell back to east. Added north/south action and reaction sheets for all six actors and made the renderer resolve every clip through the actor's current facing bank. West remains the only reflected direction.
44. Preserved 256 × 256 authoring sources while repacking the expanded cardinal rig into 128 × 128 runtime cells. The 1024 × 2560 delivery atlas reduces decoded memory from roughly 24 MiB to 10 MiB per actor, making the richer six-sheet character contract practical on mobile without discarding higher-resolution originals.
45. Replaced recovery hash equality with retained-mask RMSE plus exact geometry after Chromium filtering produced microscopic differences between duplicated atlas cells. Added a centered body-core measurement for attached effects, then expanded the public matrix to 23 runs with named north Ranger and south Arcanist action/recovery regressions. This makes the exception narrower and more explainable than relaxing global pose limits.
46. A clean 23/23 matrix from one commit received independent visual ACCEPT: cardinal actions retained facing, locomotion and camera motion stayed smooth, action/death/projectile/loot lifecycles read coherently, and desktop plus touch controls remained usable. Promoted only those reviewed baselines. The audit also found the touch-landscape selection Test Lab button extending 29 pixels past an 844-pixel viewport; constrained that layout and added an all-interactive-surfaces bounds test before release.
47. Audited generation provenance and found that every one of the 36 accepted actor source sheets is byte-identical to a preserved built-in image-generation artifact, but exact historical prompts were not retained. Added immutable hashes and artifact IDs for three complete representative actors, linked honest reconstructed briefs, and refused to invent verbatim prompts after the fact.
48. Froze exact prompts before three fresh, non-destructive generation trials: Vanguard cardinal locomotion, Ranger bow actions, and Stonekin hurt/death reactions. The generator returned 1254-pixel squares despite an exact 1024-pixel request, so the source contract now records raw dimensions and makes the existing deterministic resize-to-1024 boundary explicit.
49. Separated pipeline-proof acceptance from production-art approval. Fresh candidates may prove prompt/reference/hash capture, 4 × 4 ingress, and real packing without replacing accepted art; production promotion still requires independent visual review, runtime sprite validation, browser baselines, and the full temporal matrix.
50. Independent review vetoed all three raw generation trials even though the tolerant packer could emit valid atlas bounds. Recorded machine-readable rejection reasons for nonliteral chroma, edge contact, baseline drift, missing gait phases, oversized effects, runtime-cell mismatch, and prompt/reference identity contradictions instead of weakening the source contract.
51. Added a deterministic, non-destructive raw-to-prepared step with exact input/output hashes and commands. It keys chroma shades, removes boundary-connected neighbor fragments, applies one scale across sixteen cells, and grounds them on the declared anchor. Preparation preserved the coherent Stonekin collapse as pipeline-proof evidence but correctly left the Vanguard and Ranger semantic failures rejected.
52. Rejected the shipped mobile selector after a physical-browser screenshot showed that a green pipeline could still deliver a bad product: the title clipped, glyph copy became unreadable, repeated terrain erased depth, and tiny runtime cells sat inside oversized frames. Changed the completion criterion from “bad art was correctly rejected” to “the real player-facing screen receives visual acceptance.”
53. Generated one full-height selection scene for each hero using the same Cinderwake identity and architecture references. Preserved exact prompts, references, original bytes, artifact IDs, review notes, and deterministic 768-pixel WebP outputs; the public variants total roughly 424 KiB instead of serving 6.5 MiB of source PNGs to mobile.
54. Rebuilt selection as a single-viewport cinematic scene with compact portrait tabs, an unclipped title, and short sprite-backed controls. Selecting a class now changes the dominant art and its explicit `aria-pressed` state instead of merely adding a border to a distant card.
55. Closed the launch-path test gap. A new mobile browser test enters through the real selection page, chooses Ranger, presses the real start button, reaches the canvas, verifies the chosen class, moves, attacks, casts, and consumes a tonic. An aborted atlas request is a negative control that must produce Retry and Back instead of an endless “Waking” screen.
56. Exhaustively audited all 144 actor/clip/runtime-facing banks and 720 authored-facing phase comparisons instead of inferring atlas quality from 23 curated browser sequences. A read-only replay of commit `571909a` passed only 118/144 banks and 708/720 comparisons: the older tests falsely passed 24 reachable hurt-to-idle snaps, an Ashfang turn-scale pop, and a disconnected registered Ashfang ability pose because they did not capture those transitions. Reused exact idle cells for every hurt recovery, declared a deterministic foot-anchored Ashfang side-run transform, and held its authored ability charge before impact; the rebuilt atlases pass every bank and all four injected negative controls without relaxing a threshold. Retained 144 strips, six overviews, JSON, HTML, hashes, and exact commands under `quality-results/actor-atlas-audit/`.
57. A real phone attempt showed that tapping the world still meant Strike even though a touch joystick existed. Split world-tap movement from the explicit Strike surface and added a physical browser gesture assertion that the player keeps moving after ground contact while no attack event appears. The paired action assertion proves Strike still attacks, so the test checks intent and its forbidden inverse instead of only checking that a tap listener ran.
58. Added a four-profile public-route screen contract covering all three character choices and initial gameplay. It checks decode, viewport containment, stage occupancy, touch-target size, center hit testing, forbidden developer UI, launch liveness, and console/page errors before retaining any screenshot candidate. Head, hands, signature equipment, and feet are authored normalized landmarks; the browser maps them through the real CSS background geometry and rejects chooser/title occlusion. Deliberately undersized, offscreen, blocked, cover-cropped, and chooser-occluded mutations prove the assessor fails by name.
59. Rejected two technically valid floor candidates at different boundaries. The first generated material formed a broad orange-to-cyan band when addressed as map cells, so a uniform reference-guided source replaced it and gained regional color/luminance plus wrap-seam negative controls. Its first runtime composition still exposed a square room because cell-sized floor fragments sat over a differently scaled full-map ground image. The replacement draws one contiguous, scale-matched floor base on every cell and grades a rugged blocked-material overlay. A real-canvas test samples collision contrast and same-material joins, with invisible-boundary and exaggerated-seam mutations.
60. Preserved an evidence-backed AI game-development workflow as reusable project research. It recommends finishing Cinderwake, exercising the same four-oracle contracts on a second game, and only then extracting a package; this avoids freezing Cinderwake selectors and clip names into a framework before reuse proves the adapter boundary.
61. Independent review accepted the 16-screen public matrix after the selector retained every authored body landmark and the terrain stopped exposing a map-sized second-scale rectangle. Bound that verdict to the exact contract and ordered screenshot hashes; CI downgrades the gallery to an unapproved candidate on any mismatch. The reviewer noted that the terrain classifier's fabricated negative arrays did not prove pixel extraction, so both controls now mutate the real rerendered canvas at the actual sample points before re-extraction and named failure.
62. The first complete browser rerun exposed that legacy animation baselines captured the CSS-sized canvas element. Making the public game correctly fill a 1440 × 900 screen therefore changed a supposed 960 × 540 renderer baseline into a responsive-page screenshot. Split the evidence boundary: animation tests now compare `captureFrame()` from the fixed logical canvas, while the accepted four-profile screen matrix owns CSS scaling, cropping, overlays, and viewport composition. Updated the ordinary selector baseline only to remove the now-forbidden public Test Lab button.

## 2026-08-24

63. The clean temporal matrix passed every locomotion profile but rejected Vanguard primary and south Arcanist ability after the player was enlarged to 124 pixels: real alpha centroid step reached 24.97 against 24 and attached-effect width step reached 43 against 42. Reduced the shared player presentation to 118 pixels and made its manifest scale exactly equal destination width divided by the 256-pixel normalization contract. Both profiles pass at the original thresholds; a new mutated-manifest control fails if scale and destination width diverge again.
64. Closed the visible-object collision gap. Structures and props now come from one pure map-derived scenery layout shared by simulation and rendering, with stable IDs, anchors, explicit solid/passable policy, and elliptical world footprints around the painted ground base. Players and monsters block and slide on that same data: all raised structures, props, and rubble are solid, while only explicitly flat decals are passable; a raised-decal mutation proves the distinction. The rift exit remains enterable. Unit tests cover restored-state identity, 40 generated spawn/exit pairs, render/collision alignment, player/monster blocking, sliding, and the decal policy. A browser test injects the player beside the manifested forge, drives the real keyboard adapter for every tick, and proves both non-overlap and tangential movement.
65. Completed the clean end-to-end acceptance run after collision integration: readable source/art/generation gates passed, 71 unit tests passed, all 40 browser tests passed, the exact 16-screen hash remained independently accepted, and the full temporal catalog passed 23/23. This is the release evidence boundary for the current Cinderwake vertical slice; CI must reproduce it from the pushed commit before the public build is considered deployed.
66. Refused to treat that green release boundary as the requested 10/10 result. Fresh visual, gameplay, and architecture audits agreed that the opening still read as an empty test arena, public-loop liveness was untested, tap routes could stall at scenery, stale touch aim survived joystick takeover, and projectiles could ignore solids. Ranked the visible first engagement and ordinary-player observability ahead of broader content.
67. Generated an original 4 × 4 environmental decal source from the committed Cinderwake prop, structure, and gameplay references. The generator returned a 1,254-pixel RGB image with a baked light checker matte; preserved those raw bytes and exact prompt honestly, then made the deterministic builder key, decontaminate, recenter, pack, hash, and reject boundary contact. This keeps a useful generated candidate without pretending generation satisfied the runtime contract directly.
68. Replaced the empty generated opening with a deterministic mirrored Stonekin/Ashfang/Hexer engagement, guaranteed all fourteen monsters remain clear of scenery across eighty seeds, and projected the first three through the real portrait CSS crop. The first wide-screen candidate was independently rejected because portrait clipped the pack and the objective crossed the forge; both observations became named device-space and HUD/world-overlap gates with paired bad mutations before new candidates were retained.
69. Added an observe-only production boundary and ordinary live-player journey. It proves the launch controls enter play, real time advances ticks and fractional presentation frames, a ground tap moves without attacking, and Strike attacks without using scenario load or manual stepping. Monster health bars now derive from each current raster frame's alpha top and publish that world-UI geometry; the new gate proves the former cell-top anchor would visibly detach the Ashfang bar.
70. Made projectiles obey the same solid world as actors. Each tick now resolves the earliest radius-expanded swept contact against blocked tiles and solid scenery, consumes the shot at the exact hit point, and records an eight-tick impact effect. Friendly and hostile high-speed negative controls cross an entire forge or wall in one tick, proving an endpoint-only implementation would tunnel while the accepted implementation blocks damage and manifests the impact.
71. Submitted the changed opening screens to a fresh independent review and recorded a 4/10 rejection rather than promoting green geometry checks. The reviewer confirmed better staging and mobile containment but identified the repeating square floor grid as the dominant defect, followed by perspective/style mismatch, static tableau composition, Ashfang carcass readability, and weak mobile HUD hierarchy. The environment gate must now reject the exact visible grid failure; both Ashfang primary trials remain quarantined production rejects.
72. Rebuilt the floor from continuous source material with baked directional lighting removed and strengthened the authored blocked-edge overlays. The former four screenshots now fail a real-screen grid-salience assessor while the replacement four pass; an injected runtime-tile grid scores substantially worse and is rejected. A second independent review raised the candidate from 4/10 to 5/10, confirming the checkerboard was materially fixed while still rejecting the empty-field composition and mixed actor art.
73. Added a reusable actor-candidate calibrator that measures all sixteen source cells, shared six-family scale, grounding, idle/walk height and aspect, and loop centroid motion. It catches edge cuts, a rig-shrinking oversized frame, and a raised jump frame. Ashfang v2 passes those mechanics but remains production-rejected for glossy style, lateral camera, airborne action, and ground burst; v3 and the narrower v4 edit both fail the mechanical gate as well as visual semantics. No generated trial replaces production.
74. Made hash-matched visual rejection a first-class public report state. The exact 5/10 screen set is published as rejected rather than ambiguously “awaiting review”; any changed pixel or contract returns it to candidate. CI now publishes one quality index linking screen, temporal, browser, actor, environment, generation, and candidate-calibration evidence while explicitly refusing a 10/10 claim without visual acceptance.
75. Defined a game-neutral opening-composition adapter for topology, entrance/route, scenery clustering, crop policy, focal hierarchy, actor separation, responsive safety, and sprite provenance. Each automated signal requires a paired mutation and evidence overlay; perspective coherence, creature aliveness, environmental storytelling, dramatic hierarchy, and physical-phone glance readability retain independent-review veto power.
76. Tightened the authored opening into a bounded entrance and added a CSS-scale-aware periodic-seam gate, so the real browser composition—not just source-tile joins—must reject visible repetition. Collision now treats every raised structure, prop, and rubble pile as solid and reserves pass-through for explicitly flat decals; the raised-decal negative control catches policy drift. The isolated Ashfang pose master remains a production REJECT. Fresh review rejected the exact 16-screen matrix at 5.5/10: the bounded room is progress, but the collage still mixes camera/style/lighting, Ashfang reads as a carcass, the orange mound/coil has ambiguous raised-versus-flat affordance, and the scene lacks depth and a clear entrance story. The coordinated next slice is a single environment-kit pass—entrance wall, forge, lanterns, barricade, and raised ground clutter in one elevated perspective, palette, scale, and light direction—while preserving separate semantic sprites, matching collision footprints, and the responsive seam contract.
77. Added deterministic browser start/stop sequences for current Ashfang and Arcanist on the production-seed generated opening floor. Their command tapes require a full idle bank, exact east-facing walk start and all eight walk frames, then idle recovery with all six idle frames. Isolated foot anchors and raster bottoms are stable, but the new ≤8 logical px idle/walk median-height gate honestly rejects both current actors: Ashfang is 54/66 (12 px) and Arcanist 104/80 (24 px). A paired 9 px walk-height mutation proves the named detector independently of those known-red assets; no runtime actor art changed.
78. Promoted environment-kit v2 only after preserving its exact prompt, reference, raw bytes, hashes, and two-stage audit. The raw image remained a strict ingress reject because its matte and padding were not literal; deterministic preparation passed all six extractions and eight injected defects, and two independent reviews approved the six semantic sprites at calibrated runtime size. Generated room zero now uses the authored north wall, forge workshop, lantern pair, barricade, and raised bench with collision footprints derived from their visible contact masses.
79. The first responsive workshop grouping passed screenshot geometry but blocked the real forge-slide route. Moved the two compact props behind and toward the interior side of the forge, then made keyboard sliding, touchscreen pathfinding, projectile impact, monster pursuit, and the opening threshold derive targets from the authoritative collision layout. The browser suite now proves a real phone tap routes around at least one solid workshop sprite, reaches its world target, stops outside every collider, and returns to idle without walking in place.
80. Repaired the two start/stop failures through declared, foot-anchored rig transforms rather than a tolerance change. Ashfang idle/walk median height changed from 54/66 to 54/54; Arcanist changed from 104/80 to 104/98. Both retain zero foot-anchor and ink-bottom drift, the unchanged 8 px gate passes, the exhaustive atlas audit covers 144 banks and 720 facing comparisons, and five injected transform defects are rejected.
81. Independent pixel review scored the compact workshop scene 7/10 and rejected it because the Hexer merged with the hero and right-side props. A new placement oracle uses the sprite catalog's exact logical sizes and anchors to measure actor/player, actor/actor, and actor/compact-scenery overlap across eighty seeds. The 7 × 7 authored room could not place the hero, three opening enemies, forge, two solid props, and two doorway lanterns while satisfying mobile crop, reachability, and occlusion limits; the opening therefore starts with a readable Stonekin/Ashfang pair while Hexers remain in the generated dungeon population. This removes a 44% Hexer/barricade overlap instead of weakening the 20% scenery gate.
82. The project-wide check correctly rejected the Ashfang uniform-transform experiment after the production atlas changed: its source hash and inverse transform still described the superseded 1.42 walk stretch. Rebound the diagnostic to the exact new atlas hash and declared 1.17 transform, then reproduced the intended rejection across 390 uniform envelopes with all three bad-transform controls caught. Historical diagnostic evidence may remain rejected, but it may never remain stale.
83. Submitted the exact post-occlusion gameplay pixels to a fresh independent review. The score rose from 7.0 to 7.8/10, and the reviewer explicitly confirmed that actor/scenery overlap and collision readability are resolved across all four viewports. The hash-bound screen set remains a production REJECT because Ashfang still reads prone, black wall bays look unfinished, actors are softer than the workshop, health lines are under-styled, and wide profiles waste ground. Those findings remain the next visual acceptance targets; machine success does not promote the set.
84. Replaced the crushed enemy health lines with explicit atlas-backed world-UI calls. The renderer previously scaled two padded 256 × 256 UI cells into a 42–54 × 12 rectangle even though the painted frame occupies only 256 × 82 and the fill 256 × 48; the manifest now resolves those tight crops, preserves the frame's authored aspect at 72–88 logical pixels, and records separate frame and health-dependent fill source/destination rectangles. The sprite contract rejects missing, substituted, out-of-bounds, empty, or frame-detached references, while the browser proves both referenced sprites produce real `drawImage` calls and remain attached to the actor's current alpha top.
85. Froze a fifth Ashfang primary prompt before generation and preserved three hash-bound local references plus immutable raw and prepared bytes. The elevated camera and compact living silhouette are visibly closer to the target, but the data-driven character gate still rejects it without tolerance changes: projected idle height is 66–69 px with a 67 px median, and the 67/77 px idle-walk median delta is 10 px. All three injected cut, scale, and grounding defects remain detected. The failed candidate stays quarantined and cannot trigger generation of the other five actor families; the candidate report now renders each trial's actual recorded and mechanical reasons instead of stale v2-specific prose.
86. Traced the rectangular black workshop bays to four legacy full-cell wall facades layered beside the authored north wall. Suppressed every legacy facade whenever that coherent wall feature exists while retaining the blocked map shell and its thin sprite caps, so appearance changes without weakening actor or projectile collision. The opening contract now derives the complete blocked south-facing shell from manifested wall topology, requires a visible correctly sourced cap on every tile, forbids any legacy facade, and rejects named bay-reinsertion, missing-cap, and wrong-cap mutations alongside the existing repeated/stretched-wall controls.
87. Narrowed Ashfang v6 to the exact v5 idle-envelope failure and again froze the prompt before generation. The resulting planted sheet moves the start/stop delta from 10 px to 5.5 px and the minimum idle height from 66 px to 74 px; grounding, safe bounds, aspect, loop continuity, and all three mutations pass. The unchanged gate still rejects its 74.5 px idle median against the 76 px floor. A visually improved near miss is not rounded up, and no tolerance or per-clip scale override authorizes it.
88. The v7 source passes the real 16-cell geometry, grounding, idle/walk envelope, and loop checks, but its first audit exposed a false negative-control dependency: the oversized-action mutation was applied to each candidate, so a stronger baseline could change which threshold name fired and an already-red baseline could appear to detect a defect it already had. Rebound all three mutations to the hash-recorded mechanically green v2 fixture and require that fixture to pass before mutation. Candidate quality and detector sensitivity are now independent evidence rather than one circular result.
89. The first post-health recaptures exposed two successively deeper false greens. Call-count and rectangle-contract checks first approved invisible sprites because the tight health frame/fill heights retained cell-local Y zero even though alpha begins at Y 95 and 122. Fixing Y revealed that nominal 256-pixel cells also contain spill from adjacent top-row art, producing detached red islands. The final registrations use connected-component bounds at frame `(583,95,197×82)` and fill `(294,122,254×48)`, preserve the frame's real aspect, and run a browser alpha-component oracle over every manifested crop. A `drawImage` call, nonzero alpha, or nominal cell boundary alone is no longer evidence of one coherent visible sprite.
90. Independent review accepted v7's living-predator silhouette, elevated camera, identity, quiet idle, grounded attack, and restrained ability at actual runtime scale, but vetoed the primary family because walk cells 4–7 retain the same central support paw and merged far-side limbs. Numeric continuity cannot prove gait semantics. Recorded the exact prepared hash and per-axis verdict, kept production unchanged, moved the default candidate report from historical v2 to v7, and prohibited later-family generation until a row-two-only edit demonstrates alternating diagonal support and visible weight transfer.
91. Ashfang v8 targeted only the rejected walk semantics with explicit per-cell diagonal support ownership. Its poses show stronger paw alternation and torso transfer, but the generator lowered the complete walk bank to 71–73 px against v7's 80.5 px idle median. The unchanged gate rejects the 9 px start/stop mismatch before visual review even though grounding, aspect, loop continuity, and all three fixture-bound mutations pass. The next edit must preserve v8 limb ownership while restoring v7's vertical torso envelope.
92. V9 restored the numeric height envelope but independent runtime-scale review of prepared hash `4030c6a856a1d6da812fcf79d70b3c980af1d97809359d4d1e26339534bd3b06` still found a contact-like lower-center paw sliding through all four walk cells. Converted that finding into source evidence: alpha above 96 is projected through the bottom 16-pixel contact band, and no more than 12% of occupied support columns may persist across every phase. V7 measures 30.9% and v9 measures 18/94, or 19.15%, while the green v2 fixture and differentiated v8 both measure 0%. A fourth mutation copies walk cell 4 into cells 5–7 and must fail specifically as `walk-support-contact-persistent`; all 4/4 controls pass. V9 preparation is explicitly rejected. CI now treats the run as green only when the mechanical violation set is exactly that named rejection, preparation remains rejected, and the independent review hash matches; gait semantics still retain visual veto because contact alternation alone cannot prove anatomy.
93. Hardened the shared actor collision step against tunnelling from arbitrary restored states. A first 128-unit sampling fix stopped a one-tick building crossing but failed its own adversarial audit: a normal-radius actor could cross a short near-tangent chord through a lantern while both sampled endpoints remained clear, and a safe-integer speed could make sample count unbounded. Replaced both navigation and actor movement sampling with one analytic segment predicate over radius-expanded open tile boxes and scenery ellipses, bounded by the finite map. Player movement, monster movement, crowd separation, keyboard, and touch routes now consume that predicate. A second adversarial audit found that two safe axis legs could still yield a render-interpolated diagonal chord through a building, so a tick now accepts at most one safe axis when its final visible segment is blocked, and crowd correction validates the complete previous-to-current render chord. Scenario and snapshot loaders reject actors whose current footprint or declared interpolation path crosses solid art. Negative controls cover an entire-building leap, the exact 128-unit tangent chord, the diagonal interpolation chord, and invalid loaded states; a real phone tap at the tangent case remains stationary and idle instead of crossing the visible lantern base, while every segment of a successful routed tap is also proven clear.
94. Recaptured every baseline changed by the coherent wall-bay, irregular-floor, and connected health-bar work. The first failing run exposed eight candidates; Playwright's combat test owned two later snapshots that only became reachable after its initial mismatch was updated, so those extra pixels were quarantined and independently reviewed instead of being accepted implicitly. Exact-hash review narrowly approved all 10 deterministic regression images and confirmed touch containment, solid-object legibility, removed black bays, and connected health frames. The exact public 16-screen set remains a hash-bound production REJECT at 6.7/10 because Ashfang still looks prone, wide scenes waste ground, mobile labels are tiny, red bars dominate, and still images cannot approve temporal smoothness.
95. Closed a candidate-promotion false green before introducing per-pose assembly. `--expect-assessment pass` previously bypassed both recorded preparation and exact-hash visual-review requirements, so a mechanically valid but artistically rejected sheet could exit successfully. A pass now requires accepted raw art, accepted preparation, an exact-hash independent `ACCEPT`, at least one accepted axis, and zero rejected axes; rejection mode still requires the exact named violation set and a matching `REJECT`. A dedicated mutation runs the mechanically green Ashfang v2 fixture and proves its rejected art, rejected preparation, and missing acceptance veto promotion.
96. Generated one isolated Ashfang idle master from a prompt frozen at commit `b726504`, preserving raw hash `af0850b3898b9c7a4412545ec01752bf72bd8269dddbd643edb22c4adb73cb10`, prepared hash `f9263f8e931b3d67bfcff5539e6873c763d85252f52fffb1f29ba43fa22d54a6`, and exact image-generation artifact metadata. V2 fixes the prone-corpse posture and recenters the visible support base, but it cannot seed walk generation: runtime ink is 118×99 against the 72–86 window, visible ink ends one source pixel above the anchor, only three paws remain readable, and independent exact-hash review also rejects the side-dominant camera, oversized scale, and glossy detail noise. Its run exposed a candidate-dependent contact-offset mutation that partially cancelled itself; the control now translates from a synthetic centered fixture and catches 5/5 defects for both v1 and v2. Expected mechanical rejection also requires exact violation-set equality rather than accepting extra failures.
97. Added the reusable isolated-pose assembler instead of independently safe-fitting generated frames. The strict manifest records all 16 semantics, exact actor/style/base/raw/prompt/prefix/reference hashes, generation artifacts, and fixed visual axes; raw poses enter one 1024-pixel coordinate space, receive one max-envelope uniform scale, and land on the `(128,232)` source anchor. Inherited cells retain identical pixels, cell-local scaling is schema-forbidden, and a mechanical result stays `UNREVIEWED`. A first review found and fixed a 256-pixel normalization bug that would have made ordinary 1254-pixel generator output too small; the fixture now uses true 1024-pixel isolated inputs and rejects undersized sources. Deterministic double assembly and 16/16 named mutations cover coverage, hashes, base margins, forbidden transforms, ordered references, output binding, and stale review. Full candidate, atlas, runtime, mobile, and temporal review still gate compatibility and natural motion.
98. Audited absolute scale after the first assembler commit and rejected another plausible-but-wrong normalization: measuring in 1024-space while allowing scale up to `1` would enlarge any well-framed isolated pose until one dimension filled the 256-cell safe box. The shared scale is now capped at the canonical 1024→256 factor `0.25`; it can shrink an oversized set but cannot erase authored canvas occupancy by magnifying it. The real 1024-pixel fixture asserts that ceiling directly in addition to common-scale equality, grounding, inherited-pixel equality, deterministic bytes, and the 16 named mutations.
99. Versioned single-master preparation so new art is judged at the same absolute scale as later assembly without invalidating historical v1/v2 hashes. Fresh trials pass `--preserve-framing`, which caps the normalized 1024-pixel canvas at the canonical 0.25 scale and only shrinks further for safe bounds; the default retains legacy safe-fit solely to reproduce committed evidence. A paired v2 fixture builds preserved framing twice to byte-identical 256-pixel cells and asserts exact 165×137 placement instead of the legacy magnified 236×197 box.
100.  Replaced placement-box grounding with final keyed-pixel grounding for fresh isolated poses. Background keying, feathered edges, and asymmetric feet can move the true visible support away from a geometrically centered rectangle, so both single-master preparation and multi-pose assembly now align visible ink bottom to source Y 231 and the alpha-weighted lowest eight-pixel contact centroid within half a pixel of anchor X 128 after compositing. If that translation crosses the safe box, preparation shrinks the pose and assembly shrinks every isolated cell at one common scale before rerendering. Reports retain placement and visible bounds plus contact evidence; deterministic asymmetric-support fixtures assert weighted alignment, safe fit, and common scale, while historical legacy-safe-fit hashes remain reproducible.
101.  Preserved and independently reviewed Ashfang isolated idle master v3 rather than treating green geometry as accepted art. Raw hash `e29416ddc7e4775d0207a93bbe794b0c067acc3b64e089feadb46512de727726` prepares deterministically to `7ca8fa8b6e9fd2d11fd3c7b1291f00bd781b8f741c2fd46bff32294ae02efab5`; runtime ink is 78×78, bottom and alpha-weighted support are centered, no mechanical violation fires, and 5/5 controls pass. Exact-hash review still rejects the shallow camera, narrow mass, only three readable paws, ambiguous diagonal ownership, and highlight noise. The pose audit now separates expected mechanical pass/fail from overall art rejection and deliberately fails a mechanically green trial when its visual veto is missing, closing the false green before downstream walk generation.
102.  Narrowed isolated Ashfang v4 to anatomy, camera, mass, and matte-value corrections after freezing prompt hash `9e51897daf1841d508fa9b523601039efb65ed9840dc977f505682da097a8eff`. Raw hash `5ebe7f4e9f7006c69b0c378b6857da2c844dfcd1538e4f45af95517ee6369e35` prepares to `c4831ca89448a7559a8b4ee767f5d1d6a9f7585285ef417c6c2f2d59934e0cff`; its 84×81 runtime geometry and all 5/5 controls pass, four paws exist at source scale, and mass/support improve. Independent exact-hash review still rejects it because the far forelimb connection disappears beneath the chest after runtime reduction, preventing explicit diagonal ownership; the camera remains low and specular plate edges muddy internal anatomy. This adds a runtime limb-chain criterion that source-scale paw counting cannot satisfy.
103.  Tested whether an explicit 30-pixel source-canvas channel could repair only v4's far forelimb while preserving accepted framing and mass. V5 raw hash `344b3a70b0faf8d830385ce67f64ea50c3d36d1996294f3b5f4dd6a4adf3c3e9` prepares to `f6c24aa93d74bf5f930dee99359cc6d3d940ff22fb6350215e11a6d21bc05af8`; its 82×84 mechanical envelope, exact support, and 5/5 controls pass. The generator moved the terminal paw but did not author the requested continuous shoulder-to-paw separation, so independent exact-hash review still rejects limb-chain readability, diagonal ownership, camera elevation, production mass, matte compatibility, and runtime internal detail. V6 therefore stops editing this glossy lineage and returns to accepted production-cast references for a fresh simpler construction.
104.  Regenerated isolated Ashfang v6 from the accepted production Ashfang, Stonekin, and Hexer sheets after freezing prompt hash `21cd1b6eb62164ec5f7677714c0ceaa23c5a4ce04583c417175942f87b0e1f4c`. The fresh reference strategy restores broad Ashfang mass but emits 879×538 normalized ink plus a baked shadow; safe-width preparation must shrink below canonical scale to 0.222513, producing 98×59 runtime ink at aspect 1.661. The audit reproduces exactly `runtime-height` and `runtime-aspect` with 5/5 controls. Independent exact-hash review additionally rejects the side camera, low stalking posture, merged limb chains, undefined diagonals, glossy plate noise, graded/shadowed background, and poor internal runtime readability. V7 therefore introduces a deterministic pose-layout bitmap so framing and four limb corridors are visible generation inputs rather than prose alone.
105.  Added a deterministic internal quadruped pose-layout guide after visually rejecting the first mechanically green worker draft as a flat side view. The revision fixes a diagonal elevated body top plane, hooked tail and east head, perspective-ordered far/near paws, and four nonparallel support corridors; its own fore-channel check failed once and forced another correction before promotion. Final hash `2310bccbe0cd7c336d0beae1e8e1abbad7f8e8a2f99923b7e4aba5b683941a91` rebuilds byte-identically at 1024×1024, 636×604 occupied pixels, and contact Y 799. Independent exact-hash review accepts it only as topology/camera/framing evidence and records risks of merged roots, stilt-like legs, and copied depth colors. The project-wide check now binds that visual record alongside deterministic geometry; the guide can steer generation but can never become production art.
106.  Used the accepted guide, production Ashfang, and Stonekin as role-restricted references for isolated master v7 after freezing prompt hash `4d17b31126393a9c9fe056352f7b38d1301903407656f9fbfa8ed56a32538656`. The generator transfers head/tail direction and a raised living stance but lets the side-view species sheet override the guide's envelope and support topology. Raw hash `6e769ea6b6d0696dd227e13ed940d66285b65f7c54a265c75a9248cbbebf6591` prepares to `1af009b16bad9e24f7eac2cbbf08ba713fbd914eeef8a2d6281ad271a6e57362`; runtime ink is 98×60 at aspect 1.6333, reproducing exact `runtime-height` and `runtime-aspect` with 5/5 controls. Independent exact-hash review sees only three paw terminals and rejects the missing guide corridors, elevated camera, common mass, diagonals, matte style, and runtime topology. V8 splits topology into a simple anatomy blockout before any Ashfang surface pass.
107.  Split anatomy from styling and generated a simple quadruped blockout from only the reviewed guide and Stonekin camera reference after freezing prompt hash `0595fe22d6c0a59feafc7e81b597b5d4023bcf1470d048844a762b04727db3e9`. Raw hash `6f7672c7ae8f9247401f511ff90d9d8264d6c7cb2e64600b16c7d7e8cb55ddc6` is the first candidate whose elevated broad body, four complete limb chains, gaps, near/far ordering, and diagonal ownership receive independent structural acceptance. It remains rejected as art and even as prepared pixels: magenta lighting contaminates the entire mannequin, so the real preparer deterministically sees a blank pose. A dedicated check binds prompt/reference/raw/review hashes, reproduces exact `isolated pose is blank`, and proves no partial output survives. The internal reference can seed a recolor edit but cannot enter an atlas.
108.  Froze a narrow recolor prompt before editing the structurally accepted but chroma-invalid blockout. V2 raw hash `b93ed1bfea62bf42272f5c33b74cf3663941e32f1aa4c0c8342705bae7308d27` preserves the same silhouette and four-chain topology while replacing the foreground with keyable neutral planes; it prepares deterministically to `9218e53f3177635ff67364a7a69d201f56f61f5ae592547fa8be0a061f2d0b2a`. Runtime geometry is 81×82 with exact support and 5/5 controls. Independent exact-hash review accepts elevated camera, compact mass, four chains/paws, gaps, perspective ordering, diagonals, framing, and runtime structural readability only, while rejecting production identity, organic anatomy, joints/paws, and final material. The next edit may naturalize anatomy around locked centerlines and paw contacts but cannot move or merge them.
109.  The v3 anatomy naturalization exposed a remaining candidate-dependent test fixture: centering a synthetic resize of v3's right-biased lowest contact pushed its body outside safe bounds, so the supposedly green fixture failed before any mutation ran. Replaced candidate-derived fixture pixels with one deterministic centered body and paired support image. Every trial now receives the same five edge-cut, floating-anchor, overwide, undersized, and contact-offset mutations regardless of candidate geometry. All eight committed pose trials still reproduce their exact verdicts and 40/40 controls; v3 can now reproduce its real one-pixel runtime-height rejection instead of crashing the tester.
110.  Generated anatomy blockout v3 as a naturalization-only edit after freezing prompt hash `73b0cfe15b8a9fbab7944ba00c61b78c5c8276b98cde640593d989182f1cd820`. Raw hash `0622c4b8896fc3f77aeab81619bc66443e7f43cf6f55791769abf8423dce1828` materially improves shoulders, elbows, hocks, tapered lower limbs, and three-claw paws while preserving four connected chains, gaps, elevated camera, and diagonals. It also moves the near forepaw outward and below the near hind paw; contact-aware fitting must shrink from canonical 0.25 to 0.207534, producing prepared hash `a861aaf572e350753a0c0a1d0a9c9ddd5cee503cb02b08f93e56c73f9a887e7e` and a 73×71 runtime body. Exact `runtime-height` rejection and all 5/5 candidate-independent controls pass. Independent review allows only a baseline/footprint correction before styling.
111.  Repaired only v3's support baseline after freezing prompt hash `5754c148e86d1ff806665c2c77a713986811ba6a0625dcb7a07babb8b3057b72`. V4 raw hash `bb610d0629de7fc26417dbb7a1adbe9f0917c5507463ab2a661b85cca58a59e5` prepares to `df015f25c0b64b737204e907e7462aabc81b133438dd4b0fb02b71bcd305bdbc`; its 74×79 runtime ink, aspect, grounding, centered support, safe bounds, and all 5/5 candidate-independent controls pass. Independent review of the exact raw, prepared, evidence, and scale-comparison hashes narrowly accepts the organic joints, four connected chains and terminals, two-near-paw support row, elevated camera, gaps, near/far order, diagonals, and common gameplay mass only as a locked internal topology seed. Production remains rejected: safe fitting still uses 0.231122 instead of canonical 0.25, the matte is graded, and no Ashfang identity or cast-compatible material exists. The next identity/style edit inherits topology but none of this blockout's approval.
112.  Froze identity repaint prompt hash `95e5debf417f573931312defabffbf1b3b8f08f33345d96f0f20b4c95154e1fe` before restricting v4, production Ashfang, and Stonekin to topology, identity, and cast-rendering roles. V8 raw hash `a16aa4f7065bc04a25ad07fcaad08e770861197c50e64b9cc79d8716bac620ac` prepares to `4f9aac8921d16c6cfc69d2810345f07399a335adbe9c96d6aa54809c52fcb5df`; 75×79 runtime geometry and all 5/5 controls pass, four chains and both diagonals remain readable, and Ashfang identity is recognizable. Exact-hash review nevertheless rejects the seed: an exploratory mask comparison shows the claimed immutable stencil changed, the near-fore footprint shortens, and other contours shift. Dense stone grain, fine plate borders, silver rims, and claw microdetail collapse into runtime noise and risk temporal shimmer. This converts mask preservation and detail frequency from prompt claims into required automated evidence before any walk generation.
113.  Replaced exploratory topology judgment with two deterministic boundaries. The opt-in preparer keys candidate and reviewed topology together in normalized 1024-space, preserves reference alpha, clips extra ink, fills missing foreground through fixed nearest-color propagation, and retains byte-identical legacy behavior when absent. The independent alpha-24 prepared-cell oracle reports 463 missing plus 506 extra v8 pixels, or 969 exact differences; its best one-pixel diagnostic still leaves 253 and cannot affect the verdict. Exact mask, one-pixel contour, translated-mask, stale hash/dimensions, blank inputs, missing-color fill, clipping, deterministic artifact, and legacy-hash controls all pass. V8 now mechanically reproduces named `topology-mask-drift` in addition to its exact visual veto, and the project-wide check runs both topology suites before pose trials.
114.  The first real normalized-stencil use caught a boundary the synthetic fixture missed: v9 reproduced v4's 1024-space mask exactly, but candidate color/keying, resampling, and contact fitting changed the final prepared scale and contour. Added `--prepared-topology-mask` rather than relaxing the oracle. It preserves the reviewed cell's complete effective keyed-alpha field after placement, validates exact 256 × 256 dimensions, safe bounds, Y231 grounding, and centered contact, and reports final mismatches. Actual v9 now returns v4's 148×156 prepared bounds and −0.448901 contact offset with zero final mask or alpha mismatches; realistic missing/extra pixels, fallback fill, wrong-size, blank, stale, unsafe, ungrounded, off-center, deterministic bytes, and all historical hashes pass.
115.  Converted v8's muddy runtime-material finding into a separate finished-surface contract without polluting geometry violations. The exact keyed→Lanczos→keyed 128-pixel projection measures an alpha-192 two-pixel-eroded interior, Rec.709 high-pass after a fixed binomial blur, strong occupancy, readability, and isolated extrema. Hash-bound Vanguard/Ranger/Stonekin frames pass at strong occupancy 0.393918–0.418809 and readability 0.475793–0.512226; v8 reproduces `runtime-detail-collapse` at 0.143921/0.245416. Candidate-independent accepted fixtures catch deterministic blur collapse, seeded ±24 grain overload, and dense internal-rim overload. The opt-in report emits a deterministic detail map and retains exact-hash visual veto because frequency cannot approve style.
116.  The first complete v9 assessor then rejected the two-stage prepared output as `literal-magenta-ratio`: the final alpha lock had written transparent margins, so it looked correct over a checkerboard but violated the source-cell contract. Re-encoded the reviewed effective alpha into opaque RGB chroma values instead of weakening the gate. Exact v9 now has 65,536/65,536 opaque pixels, 55,582 literal-magenta pixels (84.8114%), exact magenta corners, zero final mask mismatches, zero final alpha mismatches after re-keying, byte-identical repeats, and unchanged 148×156 bounds/contact. Synthetic plus real v8/v9 controls explicitly reject the transparent-background false green.
117.  Packaged v9 raw hash `16b3c5b3dabb05acd0155f381a9e35ae74b93e02d5b8d2c14ee2beea6907f25b` and opaque two-stage prepared hash `b131df47004ac0288ad76d546183bd5a9f84e1b743b26a3c54da1c2bf36c1b9f`. Geometry and exact topology pass at 74×79 runtime, while the finished-surface contract reproduces only `runtime-detail-collapse` at strong occupancy 0.106996 and readability 0.268595. Independent review of exact raw, prepared, pose, cast comparison, zero-diff topology, and detail-map hashes finds no stencil halo/pinhole/smear and accepts v9 only for one narrow internal-value edit. It rejects identity/phase use because the tan dorsal saddle, flat dark cylinders, missing cool plane, orange joint bracelets, and stilt anatomy read as a generic unfinished blockout. V10 may add only several filled mid-scale plates/vents and remove bracelets; topology and noise prohibitions remain locked.
118.  V10 raw hash `b79fbd89dfc3a3e8194bb79134e9b211340060703c5f889f28d367e2a0921338` and prepared hash `ace40333987b81fbf5bcd842dfa06b4d522eb105913d0752bc9a4b03494d65c3` remove orange bracelets and add a cool flank, dorsal split, and filled chest vent while preserving zero-diff topology at 74×79. The exact surface contract becomes worse: weak occupancy rises to 0.397531 while strong occupancy falls to 0.090535 and readability to 0.185497. Independent six-hash review agrees that source-scale additions collapse at runtime and rejects the value layout, identity, cast parity, phase use, and continued visual development inside the immutable blockout silhouette. V4–v10 are retired as visual references/stencils; their durable value is the four-chain/gap/diagonal/camera/grounding test contract. The next master starts with a new natural silhouette and must earn its own geometry, detail, and visual acceptance.
119.  Froze fresh-master v11 prompt hash `1523c52e1e6c06516412469907109c786845c160d96c43fd36c8a37d44abc43d` before generation and attached only the accepted Stonekin source. Production Ashfang and v4–v10 pixels were deliberately excluded because v6/v7 proved their prone lateral silhouette overrides prose and guides. Raw hash `38a8a3a04882570b9018618c3f0426e5537fcf4517fd283e2ef34e1b491fd5b1` prepares deterministically to `82c2422d2176c3b9caa8c7ddc7f1eb9568c455c4859660e4c67e5279bf7e7e82`. The result decisively improves natural joints, heavy living identity, elevated camera, and cast-compatible material language, but the exact framework rejects its 109×90 runtime height, three visible chains/paws, missing diagonal ownership, zero raw literal-magenta ratio, and surface `0.253306` strong / `0.349696` readability scores. Independent five-hash review authorizes v11 only as a role-limited identity/anatomy/material reference for one correction; its silhouette, layout, scale, support, and phases remain forbidden.
120.  V12 tests whether explicit role denial can keep v11 identity/material traits without inheriting its rejected silhouette. It cannot: raw hash `14b209939ebc891d1744b5fa9d8ab5eb99056ee230cc4c25c541a37b641b019f` prepares to `1115d656532e150587167eb199778fe7ccefd97418728f6be715915fe5313e47` and reaches a mechanically green 102×85 runtime envelope, 1.2 aspect, centered contact, and 5/5 controls, but exact review finds the same three complete routes, missing far-hind chain, undefined diagonals, and one-paw support concentration. Surface detail improves to `0.276965` strong / `0.373394` readability yet still fails, and raw literal-magenta remains zero. Both v11 and v12 pixels are now forbidden as generation references; their accepted identity, anatomy, camera, and material findings survive only as prose and gates. The next topology attempt uses no Ashfang image reference.
121.  Reference-free v13 uses only Stonekin for cast language and a shorter topology-first prompt. Raw hash `a4809c90973cd649a8de737a5344af054e3dae995fe414ce4d246bc590b071e8` prepares to `6ad3e4c625d5cb0d8a4e86c8d34d2c1c57e1e8de85d4f2f52748374c649382f9`. It is the first fresh Ashfang to pass the finished-surface contract—`0.308391` strong occupancy, `0.401811` readability, `0.013095` isolated extrema—and exact review accepts its natural anatomy, identity, camera, palette, and isolation. It still has only three complete routes/paws, no diagonals, a 110×103 oversized runtime envelope, and zero raw literal-magenta pixels. V13 therefore cannot become a visual reference or phase seed; v14 is authorized to attach only the reviewed four-corridor layout guide plus Stonekin, without exact contour locking.
