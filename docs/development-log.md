# Development log

This is the chronological record of material build decisions. Focused ADRs contain deeper trade-off analysis; this log explains how each step contributes to an autonomously assessable game.

## 2026-08-23

1. Initialized an empty workspace as a `main`-branch Git repository. This makes each coherent decision reviewable and publishable instead of leaving one opaque final dump.
2. Selected Vite, strict TypeScript, Canvas, Vitest, and Playwright. Vite keeps the browser host small; strict types stabilize JSON contracts; Canvas exposes exact render control; Vitest runs the simulation without a browser; Playwright checks the real input and pixels.
3. Pinned dependency versions and generated a lockfile. Reproducible tools and Chromium are necessary for meaningful screenshot baselines.
4. Generated original Cinderwake three-hero key art with the built-in image generator and stored it locally under `public/assets`. It gives character selection a coherent identity without introducing a network dependency or copying an existing franchise.
5. Chose code-rendered runtime actors for the first slice. Explicit shapes make every frame, pivot, hitbox, bound, and foot anchor inspectable; generated raster animation can replace them later under the same manifest contract.
6. Delegated independent game-design, engine-architecture, test-architecture, documentation/CI, browser-rendering, and test-harness passes. Bounded ownership parallelizes work without allowing agents to overwrite the same files.
7. Fixed simulation at 60 Hz and 1,024 integer units per tile. Exact tick advancement removes wall-clock flakiness; integer positions prevent long-run drift.
8. Split random state by domain and made loot entity-keyed. Cosmetic changes cannot perturb gameplay, and killing enemies in a different order cannot silently change a particular enemy's drop.
9. Implemented complete ScenarioV1 construction instead of live-state patching. Arbitrary setups remain possible while reset semantics stay reliable.
10. Added a connected seeded room-and-corridor generator with a digest. Generated-world behavior can be fuzzed across seeds and reproduced from one short value.
11. Added three player archetypes and three enemies with melee, ranged, area, projectile, health, armor, death, loot, pickup, exit, win, and loss state. This is the smallest system mix that exercises both behavior and temporal rendering.
12. Defined two observable contracts: canonical state and semantic render manifest. Pixels show appearance; manifests explain geometry and timing when appearance changes.
13. Defined exact-tick PNG sequence capture and a self-contained report. An agent or human can review motion as a strip while automated checks measure anchors, bounds, speed, frame order, and camera movement.
14. Added a browser Test Lab for scenario loading, manual stepping, JSON export, and sequence preview. The same observability used in CI is available during ordinary development.
15. Added Prettier, ESLint, strict TypeScript, and a combined source-quality command. Authored code remains readable, and the production build now disables minification and emits source maps so the public artifact is inspectable rather than only the repository source.
16. Isolated browser tests on a dedicated strict port with server reuse disabled. An earlier run had silently attached to an unrelated process on Vite's default preview port; identity and isolation now form part of the harness contract.
17. Added real keyboard and pointer adapter coverage alongside semantic bridge tests. This proves that deterministic input injection and the controls a player actually uses reach the same simulation boundary.
18. Added reviewed screenshot baselines for selection, walking, combat impact, loot, and mid-action injection. The first visual review found an overlapping selection title and stale run-log data; both were corrected before preserving the baseline.
19. Added close-up sequence reports with a visible foot-anchor crosshair and machine-readable measurements. They retain exact full frames, state history, manifest history, metadata, and a reproduction command instead of reducing temporal evidence to one screenshot.
20. Visual review found a one-frame seam where the attack clip could wrap to its opening pose before returning to idle. One-shot frames now clamp at the terminal frame, and weapon recovery converges to the idle pose; a test fixes the terminal attack frame and subsequent idle frame in the contract.
21. Added public GitHub Pages assembly. CI rebuilds the readable game, executes source/unit/browser/visual gates, captures walking and attack reports, and publishes the game, browser report, and temporal evidence from the same commit.

Future entries should record any baseline update, tolerance change, schema migration, or intentional animation change. Those decisions directly alter what the quality system accepts and therefore must never be silent.

## 2026-08-23 (continued)

22. Added exact full-`GameState` loading and reset documentation alongside ScenarioV1. Persisting a post-setup world plus its semantic tape eliminates the temptation to reproduce an intermittent defect by manually replaying setup; reset reconstructs the retained source instead of leaking live input or timers.
23. Defined presentation interpolation and camera modes as observable render data. The live display can be smooth without weakening exact-tick captures because manifests state simulation tick, presentation tick, alpha, target, and camera mode.
24. Promoted isolated transparent entity masks from a diagnostic implementation detail to quality evidence. Alpha-pixel bounds, centroid, bottom offset, and hash provide a real raster witness for pivot, silhouette, proportion, and clipping claims.
25. Documented capture profiles, reproducibility bundles, environment metadata, mobile controls/layout, and the public capture matrix. An autonomous report is useful only when a reviewer can rerun its exact state/control/frame sequence and understand what coverage it represents.
26. Ran the first 20-sequence temporal matrix. It exposed an 86-degree enemy death rotation with roughly 23 px of ground drift and a 1.3 px Hexer recovery pop. The death pose now collapses onto the foot plane, and the Hexer attack float curve returns exactly to idle; both defects became permanent profile checks.
27. Distinguished north/south hero raster silhouettes with front/back detail while keeping all four directional masks unique and grounded. This makes control direction visually legible and gives the mask-hash test a concrete contract. Reviewed and updated only the two pixel baselines affected by this intentional directional detail.
28. Reworked the portrait stage to fill the viewport above the touch controls by center-cropping the fixed logical canvas. The same canvas bounding box still maps touch aim into logical coordinates, while an automated layout assertion prevents the prior large blank gap from returning.
29. Adopted a fully sprite-driven presentation contract with title-only text, asset provenance, deterministic atlas manifests/loading, and sprite-specific temporal QA. This converts visual quality from an opaque canvas result into inspectable source frames, anchors, alpha masks, and reproduction bundles.
30. Replaced two hand-picked public captures with a cataloged 20-report CI matrix. Each entry publishes its verdict, category, profile, source scenario, report, mask/state/tape bundle, and exact reproduction command; terminal profiles show their composed win/loss page directly in the contact sheet.
31. Required an independent visual verdict after the first matrix passed 20/20. The reviewer still rejected the loss-modal timing, low-readability Ashfang/Stonekin attacks, and missing projectile-impact coverage. Automated success therefore remains necessary but cannot approve gestalt by itself.
32. Delayed the loss modal until the exact player death lock ends, preserving every death frame on the real composed page. Added deterministic target impact bursts and hurt tint, a readable Ashfang wind-up/lunge, and a Stonekin raise/slam/recoil. The one-shot profile now requires at least five distinct real raster masks, turning the readability finding into a regression gate without loosening continuity limits. Visual review accepted the new motion, after which only the combat-impact, injected-mid-action, and portrait view baselines affected by those intentional pixels were updated.
33. Added a projectile-impact scenario that retains approach through state tick 18, proves exactly one hit/effect, and requires contiguous absence from tick 19. The public matrix now has 21 entries because continuous travel and collision/despawn answer different questions and should not be conflated.
34. The first run of the new five-pose one-shot gate rejected Ranger and Arcanist primaries at 19/21 overall: both advanced through six semantic frames but produced only three distinct raster masks. Ranger now exposes a drawn-bow release and diminishing recoil, while Arcanist visibly charges, releases, and dissipates an asymmetric staff ring. Their terminal masks still equal idle exactly; the gate was kept unchanged.
35. Replaced character-specific cropping assumptions with `ActorAtlasV2`: four fixed 4 × 4 source families, shared normalization, stable bottom-center anchors, explicit facing banks, two reserved atlas rows, and build hashes. A new actor now enters the same pipeline by ID and source sheets instead of acquiring custom renderer logic.
36. Rejected cross-dissolved locomotion and action intermediates after mask strips exposed duplicate bodies, detached Ranger arrows, and recovery ghosts. Authored direction, action-detail, and reaction banks now provide discrete poses; exact primary-idle terminal frames make recovery seams testable rather than subjective.
37. Replaced repeated full-opacity floor tiles with a continuous authored ground plane and combined scenery/actor bottom-depth sorting. This keeps the scene sprite-only while removing the checkerboard floor, allowing actors to pass logically in front of or behind buildings and props.
38. Rebuilt the responsive interface from sprite panels, portraits, controls, icons, and glyphs, retaining DOM text only for titles and accessibility mirrors. Word-grouped glyph layout and portrait/landscape assertions prevent clipped labels, off-stage cards, and touch controls covering play.
39. Kept ordinary pose dimension limits and added a narrow attached-effect rule: a one-shot flash may use a larger silhouette envelope only while centroid step and acceleration stay substantially tighter. Projectile spawn/expiry metadata, camera-normalized masks, quarter-tick mobile samples, and melee subject spacing were corrected at the same evidence boundary; targeted projectile, camera, desktop action, and mobile action captures all pass.
40. A post-matrix browser run exposed that Playwright cleans its `test-results/` root and could erase finished temporal evidence. Moved sequence bundles to ignored `quality-results/sequences/`, updated reproduction paths and CI publication, and added a contract assertion that sprite-rendered HUD values expose both accessibility labels and real glyph images.
41. Baseline review found the selection portraits still cropped ActorAtlasV2 as an 8 × 8 sheet, leaking a second animation row below every hero. Corrected the CSS crop to the real 8 × 12 atlas geometry before accepting any new screenshot.
42. An independent contract audit found ordinary DOM text still visible in labels, buttons, counters, loading, outcomes, and Test Lab despite the title-only rule. Converted every non-title surface to the committed glyph atlas, synchronized an invisible accessible seed input with visible sprite glyphs, replaced the native scenario selector with a sprite chooser, and added a browser text-node audit. The selection Test Lab control now participates in mobile flow instead of covering the Arcanist card.
43. Independent sequence review reproduced a 90-degree pose snap: north/south locomotion was authored, but every attack, ability, hurt, and death clip silently fell back to east. Added north/south action and reaction sheets for all six actors and made the renderer resolve every clip through the actor's current facing bank. West remains the only reflected direction.
44. Preserved 256 × 256 authoring sources while repacking the expanded cardinal rig into 128 × 128 runtime cells. The 1024 × 2560 delivery atlas reduces decoded memory from roughly 24 MiB to 10 MiB per actor, making the richer six-sheet character contract practical on mobile without discarding higher-resolution originals.
45. Replaced recovery hash equality with retained-mask RMSE plus exact geometry after Chromium filtering produced microscopic differences between duplicated atlas cells. Added a centered body-core measurement for attached effects, then expanded the public matrix to 23 runs with named north Ranger and south Arcanist action/recovery regressions. This makes the exception narrower and more explainable than relaxing global pose limits.
46. A clean 23/23 matrix from one commit received independent visual ACCEPT: cardinal actions retained facing, locomotion and camera motion stayed smooth, action/death/projectile/loot lifecycles read coherently, and desktop plus touch controls remained usable. Promoted only those reviewed baselines. The audit also found the touch-landscape selection Test Lab button extending 29 pixels past an 844-pixel viewport; constrained that layout and added an all-interactive-surfaces bounds test before release.
47. Audited generation provenance and found that every one of the 36 accepted actor source sheets is byte-identical to a preserved built-in image-generation artifact, but exact historical prompts were not retained. Added immutable hashes and artifact IDs for three complete representative actors, linked honest reconstructed briefs, and refused to invent verbatim prompts after the fact.
48. Froze exact prompts before three fresh, non-destructive generation trials: Vanguard cardinal locomotion, Ranger bow actions, and Stonekin hurt/death reactions. The generator returned 1254-pixel squares despite an exact 1024-pixel request, so the source contract now records raw dimensions and makes the existing deterministic resize-to-1024 boundary explicit.
49. Separated pipeline-proof acceptance from production-art approval. Fresh candidates may prove prompt/reference/hash capture, 4 × 4 ingress, and real packing without replacing accepted art; production promotion still requires independent visual review, runtime sprite validation, browser baselines, and the full temporal matrix.
50. Independent review vetoed all three raw generation trials even though the tolerant packer could emit valid atlas bounds. Recorded machine-readable rejection reasons for nonliteral chroma, edge contact, baseline drift, missing gait phases, oversized effects, runtime-cell mismatch, and prompt/reference identity contradictions instead of weakening the source contract.
51. Added a deterministic, non-destructive raw-to-prepared step with exact input/output hashes and commands. It keys chroma shades, removes boundary-connected neighbor fragments, applies one scale across sixteen cells, and grounds them on the declared anchor. Preparation preserved the coherent Stonekin collapse as pipeline-proof evidence but correctly left the Vanguard and Ranger semantic failures rejected.
52. Rejected the shipped mobile selector after a physical-browser screenshot showed that a green pipeline could still deliver a bad product: the title clipped, glyph copy became unreadable, repeated terrain erased depth, and tiny runtime cells sat inside oversized frames. Changed the completion criterion from “bad art was correctly rejected” to “the real player-facing screen receives visual acceptance.”
53. Generated one full-height selection scene for each hero using the same Cinderwake identity and architecture references. Preserved exact prompts, references, original bytes, artifact IDs, review notes, and deterministic 768-pixel WebP outputs; the public variants total roughly 424 KiB instead of serving 6.5 MiB of source PNGs to mobile.
54. Rebuilt selection as a single-viewport cinematic scene with compact portrait tabs, an unclipped title, and short sprite-backed controls. Selecting a class now changes the dominant art and its explicit `aria-pressed` state instead of merely adding a border to a distant card.
55. Closed the launch-path test gap. A new mobile browser test enters through the real selection page, chooses Ranger, presses the real start button, reaches the canvas, verifies the chosen class, moves, attacks, casts, and consumes a tonic. An aborted atlas request is a negative control that must produce Retry and Back instead of an endless “Waking” screen.
56. Exhaustively audited all 144 actor/clip/runtime-facing banks and 720 authored-facing phase comparisons instead of inferring atlas quality from 23 curated browser sequences. A read-only replay of commit `571909a` passed only 118/144 banks and 708/720 comparisons: the older tests falsely passed 24 reachable hurt-to-idle snaps, an Ashfang turn-scale pop, and a disconnected registered Ashfang ability pose because they did not capture those transitions. Reused exact idle cells for every hurt recovery, declared a deterministic foot-anchored Ashfang side-run transform, and held its authored ability charge before impact; the rebuilt atlases pass every bank and all four injected negative controls without relaxing a threshold. Retained 144 strips, six overviews, JSON, HTML, hashes, and exact commands under `quality-results/actor-atlas-audit/`.
57. A real phone attempt showed that tapping the world still meant Strike even though a touch joystick existed. Split world-tap movement from the explicit Strike surface and added a physical browser gesture assertion that the player keeps moving after ground contact while no attack event appears. The paired action assertion proves Strike still attacks, so the test checks intent and its forbidden inverse instead of only checking that a tap listener ran.
58. Added a four-profile public-route screen contract covering all three character choices and initial gameplay. It checks decode, viewport containment, stage occupancy, touch-target size, center hit testing, forbidden developer UI, launch liveness, and console/page errors before retaining any screenshot candidate. Head, hands, signature equipment, and feet are authored normalized landmarks; the browser maps them through the real CSS background geometry and rejects chooser/title occlusion. Deliberately undersized, offscreen, blocked, cover-cropped, and chooser-occluded mutations prove the assessor fails by name.
59. Rejected two technically valid floor candidates at different boundaries. The first generated material formed a broad orange-to-cyan band when addressed as map cells, so a uniform reference-guided source replaced it and gained regional color/luminance plus wrap-seam negative controls. Its first runtime composition still exposed a square room because cell-sized floor fragments sat over a differently scaled full-map ground image. The replacement draws one contiguous, scale-matched floor base on every cell and grades a rugged blocked-material overlay. A real-canvas test samples collision contrast and same-material joins, with invisible-boundary and exaggerated-seam mutations.
60. Preserved an evidence-backed AI game-development workflow as reusable project research. It recommends finishing Cinderwake, exercising the same four-oracle contracts on a second game, and only then extracting a package; this avoids freezing Cinderwake selectors and clip names into a framework before reuse proves the adapter boundary.
61. Independent review accepted the 16-screen public matrix after the selector retained every authored body landmark and the terrain stopped exposing a map-sized second-scale rectangle. Bound that verdict to the exact contract and ordered screenshot hashes; CI downgrades the gallery to an unapproved candidate on any mismatch. The reviewer noted that the terrain classifier's fabricated negative arrays did not prove pixel extraction, so both controls now mutate the real rerendered canvas at the actual sample points before re-extraction and named failure.
62. The first complete browser rerun exposed that legacy animation baselines captured the CSS-sized canvas element. Making the public game correctly fill a 1440 × 900 screen therefore changed a supposed 960 × 540 renderer baseline into a responsive-page screenshot. Split the evidence boundary: animation tests now compare `captureFrame()` from the fixed logical canvas, while the accepted four-profile screen matrix owns CSS scaling, cropping, overlays, and viewport composition. Updated the ordinary selector baseline only to remove the now-forbidden public Test Lab button.

## 2026-08-24

63. The clean temporal matrix passed every locomotion profile but rejected Vanguard primary and south Arcanist ability after the player was enlarged to 124 pixels: real alpha centroid step reached 24.97 against 24 and attached-effect width step reached 43 against 42. Reduced the shared player presentation to 118 pixels and made its manifest scale exactly equal destination width divided by the 256-pixel normalization contract. Both profiles pass at the original thresholds; a new mutated-manifest control fails if scale and destination width diverge again.
64. Closed the visible-object collision gap. Structures and props now come from one pure map-derived scenery layout shared by simulation and rendering, with stable IDs, anchors, explicit solid/passable policy, and elliptical world footprints around the painted ground base. Players and monsters block and slide on that same data; only low rubble is explicitly passable, and the rift exit remains enterable. Unit tests cover restored-state identity, 40 generated spawn/exit pairs, render/collision alignment, player/monster blocking, sliding, and the rubble exception. A browser test injects the player beside the manifested forge, drives the real keyboard adapter for every tick, and proves both non-overlap and tangential movement.
65. Completed the clean end-to-end acceptance run after collision integration: readable source/art/generation gates passed, 71 unit tests passed, all 40 browser tests passed, the exact 16-screen hash remained independently accepted, and the full temporal catalog passed 23/23. This is the release evidence boundary for the current Cinderwake vertical slice; CI must reproduce it from the pushed commit before the public build is considered deployed.
